- Values DRY principles, strict TypeScript typing, and modular React component architecture; wants messy/complex logic refactored into readable, maintainable utility functions or custom hooks. Confidence: 0.8
- Treats performance as a first-class concern in Next.js work: rendering strategies (SSR/SSG/caching), React Server Components, image/asset optimization, Core Web Vitals, and avoiding N+1 queries. Confidence: 0.7
- Prefers extracting shared business rules/constants (e.g., TAX_RATE, SHIPPING_COST, order status lists) into a single `lib/` module as the single source of truth, shared by both client contexts and server API validation so the two can never drift out of sync. Confidence: 0.7
- Prefers a shared `formatBDT` currency formatting utility imported across components over scattered inline `৳${...toLocaleString()}` strings. Confidence: 0.6
- Prefers a shared `useLocalStorage` hook to DRY the near-identical load/persist logic repeated across cart/wishlist/comparison contexts, rather than each context re-implementing storage reads and writes. Confidence: 0.6
- Prefers extracting pure filtering/derivation logic (e.g., product filtering) into a shared utility used by both pages and sidebars, removing duplicated inline logic and re-declared interfaces. Confidence: 0.6
- Follows existing codebase conventions when adding new integrations: mirrors the structure of an existing helper module (e.g., new `lib/gtm.js` copy/mirroring `lib/fpixel.js`) and documents setup in the established `docs/*.md` convention (e.g., `docs/GTM_PURCHASE_SETUP.md` alongside `docs/FACEBOOK_PIXEL_*.md`) rather than introducing new ad-hoc file patterns. Confidence: 0.7
- For e-commerce/analytics snippets, writes reusable helper modules that add a client-side safety net for window globals (e.g., `window.dataLayer = window.dataLayer || []`) so events don't race the container's async load, and exposes standard event helpers (trackPurchase, trackViewItem, trackAddToCart). Confidence: 0.6
